; Don’t show PHP notices to users; log them instead
display_errors = Off
log_errors = On

; Keep errors in logs but drop deprecation spam from output
; (still logged if extensions/framework emit them via error_log)
error_reporting = E_ALL & ~E_DEPRECATED & ~E_USER_DEPRECATED
